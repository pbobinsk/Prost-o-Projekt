<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Prost-o-Projekt</title>
  <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">
  <style>
    body { font-family: sans-serif; display: grid; place-content: center; height: 100vh; margin: 0; background: #f0f0f0; }
    .welcome-box { text-align: center; padding: 2rem 4rem; background: white; border-radius: 10px; box-shadow: 0 4px 8px rgba(0,0,0,0.1); }
    h1 { color: #333; }
    p { color: #666; margin-bottom: 2rem; }
    a button { margin: 0; }
  </style>
</head>
<body>
  <div class="welcome-box">
    <h1><%= title %></h1>
    <p><%= message %></p>
    <a href="/projects"><button>Zobacz projekty</button></a>
  </div>
</body>
</html>